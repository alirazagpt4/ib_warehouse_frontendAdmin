import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: { main: '#1976d2' }, // Wohi primary blue jo aapne kaha
    secondary: { main: '#f50057' },
    background: { default: '#f4f6f8' }
  }
});
export default theme;